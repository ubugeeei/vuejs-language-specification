amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.template.vize-vdom-patch-flags-all-dynamic"
title = "Imported template input: all dynamic"
kind = "template-dom-compile"
summary = "Static template compiler conformance derived from copied input tests/fixtures/vdom/patch-flags.toml and the official vuejs/core compiler oracle."

features {
  "compiler.template"
  "imported.input"
  "imported.input.vdom"
  "imported.input.patch-flags"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/vdom/patch-flags.toml"
  source = "tests/fixtures/vdom/patch-flags.toml"
  caseName = "all dynamic"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-dom"
  operation = "compile"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/vdom/patch-flags.toml"
    cases {
      "all dynamic"
    }
  }
}

input {
  source = "<div :id=\"id\" :class=\"cls\" :style=\"style\">{{ msg }}</div>"
}

expect {
  helpers {
    new HelperExpectation {
      name = "TO_DISPLAY_STRING"
    }
    new HelperExpectation {
      name = "NORMALIZE_CLASS"
    }
    new HelperExpectation {
      name = "NORMALIZE_STYLE"
    }
    new HelperExpectation {
      name = "OPEN_BLOCK"
    }
    new HelperExpectation {
      name = "CREATE_ELEMENT_BLOCK"
    }
  }
  hoistCount = 0
  normalizedCode = #"""
  const _Vue = Vue
  
  return function render(_ctx, _cache) {
    with (_ctx) {
      const { toDisplayString: _toDisplayString, normalizeClass: _normalizeClass, normalizeStyle: _normalizeStyle, openBlock: _openBlock, createElementBlock: _createElementBlock } = _Vue
  
      return (_openBlock(), _createElementBlock("div", {
        id: id,
        class: _normalizeClass(cls),
        style: _normalizeStyle(style)
      }, _toDisplayString(msg), 15 /* TEXT, CLASS, STYLE, PROPS */, ["id"]))
    }
  }
  """#
}
