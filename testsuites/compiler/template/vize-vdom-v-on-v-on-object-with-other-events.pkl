amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.template.vize-vdom-v-on-v-on-object-with-other-events"
title = "Imported template input: v-on object with other events"
kind = "template-dom-compile"
summary = "Static template compiler conformance derived from copied input tests/fixtures/vdom/v-on.toml and the official vuejs/core compiler oracle."

features {
  "compiler.template"
  "imported.input"
  "imported.input.vdom"
  "imported.input.v-on"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/vdom/v-on.toml"
  source = "tests/fixtures/vdom/v-on.toml"
  caseName = "v-on object with other events"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-dom"
  operation = "compile"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/vdom/v-on.toml"
    cases {
      "v-on object with other events"
    }
  }
}

input {
  source = "<button v-on=\"handlers\" @click=\"extra\"></button>"
}

expect {
  helpers {
    new HelperExpectation {
      name = "TO_HANDLERS"
    }
    new HelperExpectation {
      name = "MERGE_PROPS"
    }
    new HelperExpectation {
      name = "OPEN_BLOCK"
    }
    new HelperExpectation {
      name = "CREATE_ELEMENT_BLOCK"
    }
  }
  hoistCount = 0
  normalizedCode = #"""
  const _Vue = Vue
  
  return function render(_ctx, _cache) {
    with (_ctx) {
      const { toHandlers: _toHandlers, mergeProps: _mergeProps, openBlock: _openBlock, createElementBlock: _createElementBlock } = _Vue
  
      return (_openBlock(), _createElementBlock("button", _mergeProps(_toHandlers(handlers, true), { onClick: extra }), null, 16 /* FULL_PROPS */, ["onClick"]))
    }
  }
  """#
}
