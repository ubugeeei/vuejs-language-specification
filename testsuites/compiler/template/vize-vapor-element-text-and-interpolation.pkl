amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.template.vize-vapor-element-text-and-interpolation"
title = "Imported vapor input: text and interpolation"
kind = "template-expected-snapshot"
summary = "Provisional Vapor compiler conformance derived from copied input tests/fixtures/vapor/element.toml. This suite remains snapshot-backed until an official vuejs/core Vapor oracle is vendored."

features {
  "compiler.template"
  "compiler.snapshot"
  "compiler.profile.vapor"
  "imported.input"
  "imported.input.vapor"
  "imported.input.element"
}

profile = "vapor"

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/vapor/element.toml"
  source = "tests/fixtures/vapor/element.toml"
  caseName = "text and interpolation"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "minor/vapor compiler snapshot"
  operation = "copied snapshot oracle"
  profile = "vapor"
  provisional = true
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/vapor/element.toml"
    cases {
      "text and interpolation"
    }
  }
}

input {
  source = "<div>Hello {{ name }}!</div>"
}

expect {
  normalizedCode = #"""
  import { txt as _txt, toDisplayString as _toDisplayString, setText as _setText, renderEffect as _renderEffect, template as _template } from 'vue';
  const t0 = _template("<div> </div>", true)
  
  export function render(_ctx) {
    const n0 = t0()
    const x0 = _txt(n0)
    _renderEffect(() => _setText(x0, "Hello " + _toDisplayString(_ctx.name) + "!"))
    return n0
  }
  """#
  vendoredSnapshotOutput = #"""
  import { txt as _txt, toDisplayString as _toDisplayString, setText as _setText, renderEffect as _renderEffect, template as _template } from 'vue';
  const t0 = _template("<div> </div>", true)
  
  export function render(_ctx) {
    const n0 = t0()
    const x0 = _txt(n0)
    _renderEffect(() => _setText(x0, "Hello " + _toDisplayString(_ctx.name) + "!"))
    return n0
  }
  """#
  vendoredSnapshotOptions = "vapor"
}
