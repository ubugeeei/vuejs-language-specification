amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-patches-let-variable-increment-in-handler"
title = "Imported SFC input: let variable increment in handler"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/patches.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.patches"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/patches.toml"
  source = "tests/fixtures/sfc/patches.toml"
  caseName = "let variable increment in handler"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/patches.toml"
    cases {
      "let variable increment in handler"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-patches-let-variable-increment-in-handler.vue"
  sfc = #"""
  <script setup>
  let value = $ref(10)
  </script>
  
  <template>
    <button @click="value++">{{ value }}</button>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scopedStyles = 0
    }
  normalizedCode = #"""
  
  export default {
    __name: 'compiler.sfc.vize-sfc-patches-let-variable-increment-in-handler',
    setup(__props, { expose: __expose }) {
    __expose();
  
  let value = $ref(10)
  
  const __returned__ = { get value() { return value }, set value(v) { value = v } }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  }
  """#
  templateCode = #"""
  import { toDisplayString as _toDisplayString, openBlock as _openBlock, createElementBlock as _createElementBlock } from "vue"
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return (_openBlock(), _createElementBlock("button", {
      onClick: _cache[0] || (_cache[0] = $event => ($setup.value++))
    }, _toDisplayString($setup.value), 1 /* TEXT */))
  }
  """#
}
