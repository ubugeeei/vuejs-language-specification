amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-patches-ref-with-generic-type-should-be-stripped"
title = "Imported SFC input: ref with generic type should be stripped"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/patches.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.patches"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/patches.toml"
  source = "tests/fixtures/sfc/patches.toml"
  caseName = "ref with generic type should be stripped"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/patches.toml"
    cases {
      "ref with generic type should be stripped"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-patches-ref-with-generic-type-should-be-stripped.vue"
  sfc = #"""
  <script setup lang="ts">
  import { ref } from 'vue'
  
  interface User {
    name: string
    age: number
  }
  
  const user = ref<User | null>(null)
  const users = ref<User[]>([])
  </script>
  
  <template>
    <div v-if="user">{{ user.name }}</div>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scriptSetupLang = "ts"
      scopedStyles = 0
    }
  normalizedCode = #"""
  import { defineComponent as _defineComponent } from 'vue'
  import { ref } from 'vue'
  
  interface User {
    name: string
    age: number
  }
  
  
  export default /*@__PURE__*/_defineComponent({
    __name: 'compiler.sfc.vize-sfc-patches-ref-with-generic-type-should-be-stripped',
    setup(__props, { expose: __expose }) {
    __expose();
  
  const user = ref<User | null>(null)
  const users = ref<User[]>([])
  
  const __returned__ = { user, users }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  })
  """#
  templateCode = #"""
  import { toDisplayString as _toDisplayString, openBlock as _openBlock, createElementBlock as _createElementBlock, createCommentVNode as _createCommentVNode } from "vue"
  
  const _hoisted_1 = { key: 0 }
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return ($setup.user)
      ? (_openBlock(), _createElementBlock("div", _hoisted_1, _toDisplayString($setup.user.name), 1 /* TEXT */))
      : _createCommentVNode("v-if", true)
  }
  """#
}
