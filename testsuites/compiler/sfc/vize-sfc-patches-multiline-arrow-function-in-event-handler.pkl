amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-patches-multiline-arrow-function-in-event-handler"
title = "Imported SFC input: multiline arrow function in event handler"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/patches.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.patches"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/patches.toml"
  source = "tests/fixtures/sfc/patches.toml"
  caseName = "multiline arrow function in event handler"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/patches.toml"
    cases {
      "multiline arrow function in event handler"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-patches-multiline-arrow-function-in-event-handler.vue"
  sfc = #"""
  <script setup>
  import { ref } from 'vue'
  const count = ref(0)
  </script>
  
  <template>
    <button @click="() => {
      const newValue = count.value + 1;
      count.value = newValue;
      console.log('incremented');
    }">Increment</button>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scopedStyles = 0
    }
  normalizedCode = #"""
  import { ref } from 'vue'
  
  export default {
    __name: 'compiler.sfc.vize-sfc-patches-multiline-arrow-function-in-event-handler',
    setup(__props, { expose: __expose }) {
    __expose();
  
  const count = ref(0)
  
  const __returned__ = { count, ref }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  }
  """#
  templateCode = #"""
  import { openBlock as _openBlock, createElementBlock as _createElementBlock } from "vue"
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return (_openBlock(), _createElementBlock("button", {
      onClick: _cache[0] || (_cache[0] = () => {
      const newValue = $setup.count.value + 1;
      $setup.count.value = newValue;
      console.log('incremented');
    })
    }, "Increment"))
  }
  """#
}
