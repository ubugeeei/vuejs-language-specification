amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-patches-type-comparison-should-not-be-stripped"
title = "Imported SFC input: type comparison should not be stripped"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/patches.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.patches"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/patches.toml"
  source = "tests/fixtures/sfc/patches.toml"
  caseName = "type comparison should not be stripped"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/patches.toml"
    cases {
      "type comparison should not be stripped"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-patches-type-comparison-should-not-be-stripped.vue"
  sfc = #"""
  <script setup>
  const props = defineProps(['type'])
  const isButton = props.type === 'button'
  const isLink = props.type !== 'link'
  </script>
  
  <template>
    <button v-if="isButton">Button</button>
    <a v-else-if="!isLink" href="#">Link</a>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scopedStyles = 0
    }
  normalizedCode = #"""
  
  export default {
    __name: 'compiler.sfc.vize-sfc-patches-type-comparison-should-not-be-stripped',
    props: ['type'],
    setup(__props, { expose: __expose }) {
    __expose();
  
  const props = __props
  const isButton = props.type === 'button'
  const isLink = props.type !== 'link'
  
  const __returned__ = { props, isButton, isLink }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  }
  """#
  templateCode = #"""
  import { openBlock as _openBlock, createElementBlock as _createElementBlock, createCommentVNode as _createCommentVNode } from "vue"
  
  const _hoisted_1 = { key: 0 }
  const _hoisted_2 = {
    key: 1,
    href: "#"
  }
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return ($setup.isButton)
      ? (_openBlock(), _createElementBlock("button", _hoisted_1, "Button"))
      : (!$setup.isLink)
        ? (_openBlock(), _createElementBlock("a", _hoisted_2, "Link"))
        : _createCommentVNode("v-if", true)
  }
  """#
}
