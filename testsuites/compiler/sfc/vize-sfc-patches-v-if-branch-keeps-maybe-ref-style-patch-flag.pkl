amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-patches-v-if-branch-keeps-maybe-ref-style-patch-flag"
title = "Imported SFC input: v if branch keeps maybe ref style patch flag"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/patches.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.patches"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/patches.toml"
  source = "tests/fixtures/sfc/patches.toml"
  caseName = "v if branch keeps maybe ref style patch flag"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/patches.toml"
    cases {
      "v if branch keeps maybe ref style patch flag"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-patches-v-if-branch-keeps-maybe-ref-style-patch-flag.vue"
  sfc = #"""
  <script setup lang="ts">
  import { ref } from 'vue'
  
  const show = ref(true)
  const floatingStyles = ref({ left: '10px', top: '20px' })
  </script>
  
  <template>
    <div v-if="show" :style="floatingStyles"></div>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scriptSetupLang = "ts"
      scopedStyles = 0
    }
  normalizedCode = #"""
  import { defineComponent as _defineComponent } from 'vue'
  import { ref } from 'vue'
  
  
  export default /*@__PURE__*/_defineComponent({
    __name: 'compiler.sfc.vize-sfc-patches-v-if-branch-keeps-maybe-ref-style-patch-flag',
    setup(__props, { expose: __expose }) {
    __expose();
  
  const show = ref(true)
  const floatingStyles = ref({ left: '10px', top: '20px' })
  
  const __returned__ = { show, floatingStyles }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  })
  """#
  templateCode = #"""
  import { normalizeStyle as _normalizeStyle, openBlock as _openBlock, createElementBlock as _createElementBlock, createCommentVNode as _createCommentVNode } from "vue"
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return ($setup.show)
      ? (_openBlock(), _createElementBlock("div", {
          key: 0,
          style: _normalizeStyle($setup.floatingStyles)
        }, null, 4 /* STYLE */))
      : _createCommentVNode("v-if", true)
  }
  """#
}
