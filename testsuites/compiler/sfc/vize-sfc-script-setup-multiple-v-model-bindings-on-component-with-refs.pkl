amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-script-setup-multiple-v-model-bindings-on-component-with-refs"
title = "Imported SFC input: multiple v-model bindings on component with refs"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/script-setup.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.script-setup"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/script-setup.toml"
  source = "tests/fixtures/sfc/script-setup.toml"
  caseName = "multiple v-model bindings on component with refs"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/script-setup.toml"
    cases {
      "multiple v-model bindings on component with refs"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-script-setup-multiple-v-model-bindings-on-component-with-refs.vue"
  sfc = #"""
  <script setup lang="ts">
  import { ref } from 'vue';
  import Dialog from './Dialog.vue';
  const isOpen = ref(false);
  const formValue = ref('');
  </script>
  
  <template>
    <Dialog v-model="isOpen" v-model:value="formValue" />
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scriptSetupLang = "ts"
      scopedStyles = 0
    }
  normalizedCode = #"""
  import { defineComponent as _defineComponent } from 'vue'
  import { ref } from 'vue';
  import Dialog from './Dialog.vue';
  
  export default /*@__PURE__*/_defineComponent({
    __name: 'compiler.sfc.vize-sfc-script-setup-multiple-v-model-bindings-on-component-with-refs',
    setup(__props, { expose: __expose }) {
    __expose();
  
  const isOpen = ref(false);
  const formValue = ref('');
  
  const __returned__ = { isOpen, formValue, Dialog }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  })
  """#
  templateCode = #"""
  import { openBlock as _openBlock, createBlock as _createBlock } from "vue"
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return (_openBlock(), _createBlock($setup["Dialog"], {
      modelValue: $setup.isOpen,
      "onUpdate:modelValue": _cache[0] || (_cache[0] = $event => (($setup.isOpen) = $event)),
      value: $setup.formValue,
      "onUpdate:value": _cache[1] || (_cache[1] = $event => (($setup.formValue) = $event))
    }, null, 8 /* PROPS */, ["modelValue", "value"]))
  }
  """#
}
