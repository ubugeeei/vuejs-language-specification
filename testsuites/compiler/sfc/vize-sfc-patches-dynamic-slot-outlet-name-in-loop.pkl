amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-patches-dynamic-slot-outlet-name-in-loop"
title = "Imported SFC input: dynamic slot outlet name in loop"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/patches.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.patches"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/patches.toml"
  source = "tests/fixtures/sfc/patches.toml"
  caseName = "dynamic slot outlet name in loop"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/patches.toml"
    cases {
      "dynamic slot outlet name in loop"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-patches-dynamic-slot-outlet-name-in-loop.vue"
  sfc = #"""
  <script setup>
  const tabs = [
    { key: 'basic', label: 'Basic' },
    { key: 'parts', label: 'Parts' },
  ]
  </script>
  
  <template>
    <div>
      <template v-for="tab in tabs" :key="tab.key">
        <slot :name="tab.key" :tab="tab" />
      </template>
    </div>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scopedStyles = 0
    }
  normalizedCode = #"""
  
  export default {
    __name: 'compiler.sfc.vize-sfc-patches-dynamic-slot-outlet-name-in-loop',
    setup(__props, { expose: __expose }) {
    __expose();
  
  const tabs = [
    { key: 'basic', label: 'Basic' },
    { key: 'parts', label: 'Parts' },
  ]
  
  const __returned__ = { tabs }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  }
  """#
  templateCode = #"""
  import { renderList as _renderList, Fragment as _Fragment, openBlock as _openBlock, createElementBlock as _createElementBlock, renderSlot as _renderSlot } from "vue"
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return (_openBlock(), _createElementBlock("div", null, [
      (_openBlock(), _createElementBlock(_Fragment, null, _renderList($setup.tabs, (tab) => {
        return _renderSlot(_ctx.$slots, tab.key, {
          key: tab.key,
          tab: tab
        })
      }), 64 /* STABLE_FRAGMENT */))
    ]))
  }
  """#
}
