amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-script-setup-script-with-interfaces-before-script-setup"
title = "Imported SFC input: script with interfaces before script setup"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/script-setup.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.script-setup"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/script-setup.toml"
  source = "tests/fixtures/sfc/script-setup.toml"
  caseName = "script with interfaces before script setup"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/script-setup.toml"
    cases {
      "script with interfaces before script setup"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-script-setup-script-with-interfaces-before-script-setup.vue"
  sfc = #"""
  <script lang="ts">
  export interface User {
    id: number;
    name: string;
  }
  </script>
  
  <script setup lang="ts">
  import { ref } from 'vue'
  const user = ref<User | null>(null)
  </script>
  
  <template>
    <div v-if="user">{{ user.name }}</div>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = true
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scriptLang = "ts"
      scriptSetupLang = "ts"
      scopedStyles = 0
    }
  normalizedCode = #"""
  import { defineComponent as _defineComponent } from 'vue'
  import { ref } from 'vue'
  
  export interface User {
    id: number;
    name: string;
  }
  
  export default /*@__PURE__*/_defineComponent({
    __name: 'compiler.sfc.vize-sfc-script-setup-script-with-interfaces-before-script-setup',
    setup(__props, { expose: __expose }) {
    __expose();
  
  const user = ref<User | null>(null)
  
  const __returned__ = { user }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  })
  """#
  templateCode = #"""
  import { toDisplayString as _toDisplayString, openBlock as _openBlock, createElementBlock as _createElementBlock, createCommentVNode as _createCommentVNode } from "vue"
  
  const _hoisted_1 = { key: 0 }
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return ($setup.user)
      ? (_openBlock(), _createElementBlock("div", _hoisted_1, _toDisplayString($setup.user.name), 1 /* TEXT */))
      : _createCommentVNode("v-if", true)
  }
  """#
}
