amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-patches-es6-shorthand-in-event-handler-object"
title = "Imported SFC input: ES6 shorthand in event handler object"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/patches.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.patches"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/patches.toml"
  source = "tests/fixtures/sfc/patches.toml"
  caseName = "ES6 shorthand in event handler object"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/patches.toml"
    cases {
      "ES6 shorthand in event handler object"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-patches-es6-shorthand-in-event-handler-object.vue"
  sfc = #"""
  <script setup>
  import { ref } from 'vue'
  const foo = ref('value')
  const bar = ref(123)
  </script>
  
  <template>
    <div @click="handleEvent({ foo, bar })">Test</div>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scopedStyles = 0
    }
  normalizedCode = #"""
  import { ref } from 'vue'
  
  export default {
    __name: 'compiler.sfc.vize-sfc-patches-es6-shorthand-in-event-handler-object',
    setup(__props, { expose: __expose }) {
    __expose();
  
  const foo = ref('value')
  const bar = ref(123)
  
  const __returned__ = { foo, bar, ref }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  }
  """#
  templateCode = #"""
  import { openBlock as _openBlock, createElementBlock as _createElementBlock } from "vue"
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return (_openBlock(), _createElementBlock("div", {
      onClick: _cache[0] || (_cache[0] = $event => (_ctx.handleEvent({ foo: $setup.foo, bar: $setup.bar })))
    }, "Test"))
  }
  """#
}
