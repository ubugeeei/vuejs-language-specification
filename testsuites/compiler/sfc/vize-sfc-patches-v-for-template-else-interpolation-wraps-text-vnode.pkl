amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-patches-v-for-template-else-interpolation-wraps-text-vnode"
title = "Imported SFC input: v for template else interpolation wraps text vnode"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/patches.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.patches"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/patches.toml"
  source = "tests/fixtures/sfc/patches.toml"
  caseName = "v for template else interpolation wraps text vnode"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/patches.toml"
    cases {
      "v for template else interpolation wraps text vnode"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-patches-v-for-template-else-interpolation-wraps-text-vnode.vue"
  sfc = #"""
  <script setup>
  function splitByQuery(name) {
    return [{ text: name, match: false }]
  }
  const name = 'paper'
  </script>
  
  <template>
    <p>
      <template v-for="(seg, i) in splitByQuery(name)" :key="i">
        <mark v-if="seg.match">{{ seg.text }}</mark>
        <template v-else>{{ seg.text }}</template>
      </template>
    </p>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scopedStyles = 0
    }
  normalizedCode = #"""
  const name = 'paper'
  
  export default {
    __name: 'compiler.sfc.vize-sfc-patches-v-for-template-else-interpolation-wraps-text-vnode',
    setup(__props, { expose: __expose }) {
    __expose();
  
  function splitByQuery(name) {
    return [{ text: name, match: false }]
  }
  
  const __returned__ = { splitByQuery, name }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  }
  """#
  templateCode = #"""
  import { renderList as _renderList, Fragment as _Fragment, openBlock as _openBlock, createElementBlock as _createElementBlock, toDisplayString as _toDisplayString, createCommentVNode as _createCommentVNode, createTextVNode as _createTextVNode } from "vue"
  
  const _hoisted_1 = { key: 0 }
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return (_openBlock(), _createElementBlock("p", null, [
      (_openBlock(true), _createElementBlock(_Fragment, null, _renderList($setup.splitByQuery($setup.name), (seg, i) => {
        return (_openBlock(), _createElementBlock(_Fragment, { key: i }, [
          (seg.match)
            ? (_openBlock(), _createElementBlock("mark", _hoisted_1, _toDisplayString(seg.text), 1 /* TEXT */))
            : (_openBlock(), _createElementBlock(_Fragment, { key: 1 }, [
                _createTextVNode(_toDisplayString(seg.text), 1 /* TEXT */)
              ], 64 /* STABLE_FRAGMENT */))
        ], 64 /* STABLE_FRAGMENT */))
      }), 128 /* KEYED_FRAGMENT */))
    ]))
  }
  """#
}
