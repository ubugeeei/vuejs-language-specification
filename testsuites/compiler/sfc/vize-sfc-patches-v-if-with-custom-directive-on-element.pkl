amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-patches-v-if-with-custom-directive-on-element"
title = "Imported SFC input: v-if with custom directive on element"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/patches.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.patches"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/patches.toml"
  source = "tests/fixtures/sfc/patches.toml"
  caseName = "v-if with custom directive on element"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/patches.toml"
    cases {
      "v-if with custom directive on element"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-patches-v-if-with-custom-directive-on-element.vue"
  sfc = #"""
  <script setup>
  import { ref } from 'vue'
  const showHelp = ref(true)
  const helpText = ref('Help')
  </script>
  
  <template>
    <button v-if="showHelp" v-tooltip.noDelay="helpText">Help</button>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scopedStyles = 0
    }
  normalizedCode = #"""
  import { ref } from 'vue'
  
  export default {
    __name: 'compiler.sfc.vize-sfc-patches-v-if-with-custom-directive-on-element',
    setup(__props, { expose: __expose }) {
    __expose();
  
  const showHelp = ref(true)
  const helpText = ref('Help')
  
  const __returned__ = { showHelp, helpText, ref }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  }
  """#
  templateCode = #"""
  import { createTextVNode as _createTextVNode, resolveDirective as _resolveDirective, openBlock as _openBlock, createElementBlock as _createElementBlock, withDirectives as _withDirectives, createCommentVNode as _createCommentVNode } from "vue"
  
  const _hoisted_1 = { key: 0 }
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    const _directive_tooltip = _resolveDirective("tooltip")
  
    return ($setup.showHelp)
      ? _withDirectives((_openBlock(), _createElementBlock("button", _hoisted_1, [...(_cache[0] || (_cache[0] = [
          _createTextVNode("Help", -1 /* CACHED */)
        ]))])), [
          [
            _directive_tooltip,
            $setup.helpText,
            void 0,
            { noDelay: true }
          ]
        ])
      : _createCommentVNode("v-if", true)
  }
  """#
}
