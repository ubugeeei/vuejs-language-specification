amends "../../../schemas/CompilerTestSuite.pkl"

id = "compiler.sfc.vize-sfc-script-setup-reco-guidanceprogresslapinputbtn-pattern"
title = "Imported SFC input: reco GuidanceProgressLapInputBtn pattern"
kind = "sfc-full-compile"
summary = "Static SFC compiler pipeline conformance derived from copied input tests/fixtures/sfc/script-setup.toml and the official vuejs/core SFC oracle."

features {
  "compiler.sfc"
  "imported.input"
  "imported.input.sfc"
  "imported.input.script-setup"
}

inputOrigin = new ImportedInputOrigin {
  copiedPath = "provenance/vendor/ubugeeei-vize/tests/fixtures/sfc/script-setup.toml"
  source = "tests/fixtures/sfc/script-setup.toml"
  caseName = "reco GuidanceProgressLapInputBtn pattern"
  originRepository = "ubugeeei/vize"
}

oracle = new ReferenceOracle {
  repository = "vuejs/core"
  moduleName = "@vue/compiler-sfc"
  operation = "parse + compileScript + compileTemplate + compileStyle"
}

upstream {
  new UpstreamReference {
    repository = "ubugeeei/vize"
    source = "tests/fixtures/sfc/script-setup.toml"
    cases {
      "reco GuidanceProgressLapInputBtn pattern"
    }
  }
}

input {
  filename = "compiler.sfc.vize-sfc-script-setup-reco-guidanceprogresslapinputbtn-pattern.vue"
  sfc = #"""
  <script setup lang="ts">
  import { computed } from 'vue';
  
  interface GuidanceProgressFormInput {
    progressLap: number;
    originalProgressLap: number;
    progressUnit: { value: string };
  }
  
  interface Props {
    guidanceProgress: GuidanceProgressFormInput;
    disabled?: boolean;
    readonly?: boolean;
  }
  const props = defineProps<Props>();
  
  const lapClass = computed(() => `lap-${props.guidanceProgress.progressLap}`);
  const isChangedProgressLap = computed(
    () => props.guidanceProgress.progressLap !== props.guidanceProgress.originalProgressLap
  );
  </script>
  
  <template>
    <button
      :class="[{ disabled }, lapClass]"
      :disabled="disabled"
      @click.prevent
    >
      {{ guidanceProgress.progressUnit.value }}
    </button>
  </template>
  
  """#
}

expect {
    descriptor = new DescriptorExpectation {
      template = true
      script = false
      scriptSetup = true
      styles = 0
      customBlocks = 0
      scriptSetupLang = "ts"
      scopedStyles = 0
    }
  normalizedCode = #"""
  import { defineComponent as _defineComponent } from 'vue'
  import { computed } from 'vue';
  
  interface GuidanceProgressFormInput {
    progressLap: number;
    originalProgressLap: number;
    progressUnit: { value: string };
  }
  
  interface Props {
    guidanceProgress: GuidanceProgressFormInput;
    disabled?: boolean;
    readonly?: boolean;
  }
  
  export default /*@__PURE__*/_defineComponent({
    __name: 'compiler.sfc.vize-sfc-script-setup-reco-guidanceprogresslapinputbtn-pattern',
    props: {
      guidanceProgress: { type: Object, required: true },
      disabled: { type: Boolean, required: false },
      readonly: { type: Boolean, required: false }
    },
    setup(__props: any, { expose: __expose }) {
    __expose();
  
  const props = __props;
  
  const lapClass = computed(() => `lap-${props.guidanceProgress.progressLap}`);
  const isChangedProgressLap = computed(
    () => props.guidanceProgress.progressLap !== props.guidanceProgress.originalProgressLap
  );
  
  const __returned__ = { props, lapClass, isChangedProgressLap }
  Object.defineProperty(__returned__, '__isScriptSetup', { enumerable: false, value: true })
  return __returned__
  }
  
  })
  """#
  templateCode = #"""
  import { toDisplayString as _toDisplayString, withModifiers as _withModifiers, normalizeClass as _normalizeClass, openBlock as _openBlock, createElementBlock as _createElementBlock } from "vue"
  
  const _hoisted_1 = ["disabled"]
  
  export function render(_ctx, _cache, $props, $setup, $data, $options) {
    return (_openBlock(), _createElementBlock("button", {
      class: _normalizeClass([{ disabled: $props.disabled }, $setup.lapClass]),
      disabled: $props.disabled,
      onClick: _cache[0] || (_cache[0] = _withModifiers(() => {}, ["prevent"]))
    }, _toDisplayString($props.guidanceProgress.progressUnit.value), 11 /* TEXT, CLASS, PROPS */, _hoisted_1))
  }
  """#
}
